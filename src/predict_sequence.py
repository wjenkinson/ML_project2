"""Generate sequence predictions using the trained physics-informed model.

Expected responsibilities:
- Load trained model checkpoints
- Run inference over validation / test sequences
- Save predicted sequences for downstream post-processing
"""
