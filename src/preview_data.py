"""Preview raw simulation data to verify format, ranges, and basic sanity checks.

Expected responsibilities:
- Load a small subset of the LAMMPS data
- Produce minimal visual/summary outputs to confirm data is as expected
"""
